{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build && node postbuild.mjs",
    "dev": "nuxt dev --host",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "h3": "jiti h3Standalone.ts"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "jiti": "^1.19.3",
    "nuxt": "^3.7.0",
    "postgraphile": "5.0.0-beta.7"
  }
}
