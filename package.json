{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build && node postbuild.mjs",
    "dev": "nuxt dev --host",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "h3": "node -r jiti/register -r dotenv/config h3Standalone.ts",
    "export-schema": "node -r jiti/register -r dotenv/config export-schema.ts"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "dotenv": "^16.3.1",
    "eslint-plugin-graphile-export": "0.0.2-beta.1",
    "graphile-export": "0.0.2-beta.1",
    "jiti": "^1.19.3",
    "nuxt": "^3.7.0",
    "postgraphile": "5.0.0-beta.7"
  }
}
